#include "WCP2dToy/ToyMatrix.h"

#include "TDecompSVD.h"

using namespace WCP;

/**
 * @brief Judges whether a cell is a simple blob in the ToyMatrix.
 *
 * This function iterates through all cells in the MergeToyTiling and checks if each cell's charge is above the reconstruction threshold.
 * If the charge is above the threshold, it finds the edges of the cell and checks if the cell is a blob.
 * For blobs, it finds the corners, calculates the number of wires and cells connected to the blob, and determines if the blob is simple based on these numbers.
 *
 * @param toytiling The ToyTiling object containing the cell map and wire map.
 * @param mergetiling The MergeToyTiling object containing the merged cell selection and wire selection.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
void WCP2dToy::ToyMatrix::JudgeSimpleBlob(WCP2dToy::ToyTiling& toytiling, WCP2dToy::MergeToyTiling& mergetiling){
   GeomCellSelection allmcell = mergetiling.get_allcell();
   num_blob = 0;
   for (int j=0;j!=allmcell.size();j++){
     MergeGeomCell *mcell =(MergeGeomCell*)allmcell.at(j);
     
     double charge =Get_Cell_Charge(mcell);
     if (charge>recon_threshold){
       mcell->FindEdges();
       // std::cout << mcell->get_allcell().size() << " " << mcell->get_edgecells().size() << std::endl;
       int nwire = 0, max_wire = 0;
       int ncell = 0;
       if(mcell->IsBlob()) {
	 mcell->FindCorners(toytiling.cmap(), toytiling.wmap());
	 num_blob ++; 
	 // GeomWireSelection n_mwires = mergetiling[i]->wires(*mcell);
	 // for (int k=0;k!=n_mwires.size();k++){
	 //   int ncells = 0;
	 //   for (int kk= 0; kk!=mergetiling[i]->cells(*n_mwires.at(k)).size();kk++){
	 //     if (toymatrix[i]->Get_Cell_Charge(mergetiling[i]->cells(*n_mwires.at(k)).at(kk))>recon_threshold){
	 // 	ncells ++;
	 //     }
	 //   }
	 //   std::cout << ncells << std::endl;
	 
	 // find simple blob ... 
	 // find the merged wire regarding the merged blob
	 GeomWireSelection wires = mergetiling.wires(*mcell);
	 // find the number of wires 
	 for (int k =0; k!=wires.size();k++){
	   MergeGeomWire* mwire = (MergeGeomWire*)wires.at(k);
	   //std::cout << mwire->get_allwire().size() << std::endl;
	   nwire += mwire->get_allwire().size();
	   if (mwire->get_allwire().size() > max_wire) 
	     max_wire = mwire->get_allwire().size();
	   GeomCellSelection cells = mergetiling.cells(*mwire);
	   for (int kk=0; kk!= cells.size(); kk++){
	     if (cells.at(kk) != mcell && Get_Cell_Charge(cells.at(kk))>recon_threshold){
	       ncell += ((MergeGeomCell*)cells.at(kk))->get_allcell().size();
	     }
	   }
	 }
	 nwire -= max_wire;
	 // for the merged blob passed the cut, find the number of cells
	 //std::cout << "Xin: " << nwire << " " << max_wire << " " << ncell << std::endl;
	 if (ncell < nwire){
	   mcell->SetSimpleBlob(true);
	   simple_blob_reduction = true;
	 }else{
	   mcell->SetSimpleBlob(false);
	 }
       }
     }
     
     
    // 	GeomCellSelection corners = mcell->get_cornercells();
    // 	total_corner_cells.insert(total_corner_cells.end(),corners.begin(),corners.end());
    // 	for (int k=0;k!=mcell->get_allcell().size();k++){
    // 	  total_recon_cells.push_back(mcell->get_allcell().at(k));
    // 	  //get charge
    // 	  double sc_charge = 0;
    // 	  GeomCellMap scmap = toytiling[i]->cmap();
    // 	  WireChargeMap wcmap = toytiling[i]->wcmap();
    // 	  GeomWireSelection wires = scmap[mcell->get_allcell().at(k)];
    // 	  double aa[3];
    // 	  for (int q=0;q!=wires.size();q++){
    // 	    sc_charge += wcmap[wires.at(q)];
    // 	    aa[q] = wcmap[wires.at(q)];
    // 	  }
    // 	  total_scmap[mcell->get_allcell().at(k)] = sc_charge/3;
    // 	  total_scrms[mcell->get_allcell().at(k)] = rms(aa[0],aa[1],aa[2])*3./sc_charge;
    // 	  if (sc_charge/3 > charge_max) charge_max = sc_charge/3;
    // 	  if (sc_charge/3 < charge_min) charge_min = sc_charge/3;
    // 	}
    //   }
   }

    // toymatrix[i]->Set_blob(num_blob);
    
    // cout << "# of blobs " << toymatrix[i]->Get_blob() << endl;

}

/**
 * @brief Constructor for ToyMatrix class.
 * Initializes member variables and allocates memory for matrices and vectors.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
WCP2dToy::ToyMatrix::ToyMatrix(){
  solve_flag = -1;
  chi2 = -1;
  svd_flag = 0;
  num_blob = 0;
  simple_blob_reduction = false;
  recon_threshold = 2000;
  
  mwindex = 1;
  mcindex = 1;

  MA = new TMatrixD(mwindex,mcindex);
  MB = new TMatrixD(mwindex,swindex);
  MAT = new TMatrixD(mcindex,mwindex);
  MBT = new TMatrixD(swindex,mwindex);
  
  Vy = new TMatrixD(swindex,swindex);
  VBy = new TMatrixD(mwindex,mwindex);
  VBy_inv = new TMatrixD(mwindex,mwindex);
  Vx = new TMatrixD(mcindex,mcindex);
  Vx_inv = new TMatrixD(mcindex,mcindex);
  
  MC = new TMatrixD(mcindex,mcindex);
  MC_inv = new TMatrixD(mcindex,mcindex);
  
  //Construct Vector
  Wy = new TVectorD(swindex);
  
  MWy = new TVectorD(mwindex);
  MWy_pred = new TVectorD(mwindex);
  
  Cx = new TVectorD(mcindex);
  dCx = new TVectorD(mcindex);
  
}


/**
 * @brief Constructor for ToyMatrix class.
 * 
 * @param toytiling Reference to ToyTiling object.
 * @param mergetiling Reference to MergeToyTiling object.
 * @param svd_flag1 Integer flag for SVD.
 * @param recon_t Reconstruction threshold.
  
 * @brief Builds up index for ToyMatrix.
 * 
 * @param mergetiling Reference to MergeToyTiling object.
 
void Buildup_index(MergeToyTiling& mergetiling)
 * @brief Constructs matrix for ToyMatrix.
 
void ConstructMatrix()
 * @brief Solves the system of equations.
 
void Solve()
 * @brief Solves the system of equations using SVD.
 
void Solve_SVD()
 * @brief Prints the matrix.
 
void PrintMatrix()* This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
WCP2dToy::ToyMatrix::ToyMatrix(WCP2dToy::ToyTiling& toytiling, WCP2dToy::MergeToyTiling& mergetiling, int svd_flag1, int recon_t){
  solve_flag = -1;
  chi2 = -1;
  svd_flag = svd_flag1;
  num_blob = 0;
  simple_blob_reduction = false;
  recon_threshold = recon_t;
  
  // build up index
  Buildup_index(mergetiling);
  
  //std::cout << mcindex << " " << mwindex << " " << swindex << std::endl;
  if (mcindex >0){
  //Construct Matrix
    MA = new TMatrixD(mwindex,mcindex); // merged wire to merged cell
    MB = new TMatrixD(mwindex,swindex); // merged wire to single wire (single channel?)
    MAT = new TMatrixD(mcindex,mwindex); 
    MBT = new TMatrixD(swindex,mwindex);
    
    Vy = new TMatrixD(swindex,swindex); // single wire (single channel)
    VBy = new TMatrixD(mwindex,mwindex);
    VBy_inv = new TMatrixD(mwindex,mwindex);
    Vx = new TMatrixD(mcindex,mcindex);
    Vx_inv = new TMatrixD(mcindex,mcindex);
    
    MC = new TMatrixD(mcindex,mcindex);
    MC_inv = new TMatrixD(mcindex,mcindex);
    
    //Construct Vector
    Wy = new TVectorD(swindex);
    
    MWy = new TVectorD(mwindex);
    MWy_pred = new TVectorD(mwindex);

    Cx = new TVectorD(mcindex);
    dCx = new TVectorD(mcindex);
    
    GeomWireSelection allwire = toytiling.get_allwire();
    WireChargeMap wcmap = toytiling.wcmap();
    WireChargeMap wcemap = toytiling.wcemap();
    
    for (int j=0;j!=allwire.size();j++){
      if (swimap.find(allwire[j])!=swimap.end()){
	int index = swimap[allwire[j]];
	float charge = wcmap[allwire[j]];
	float charge_err = wcemap[allwire[j]];
	(*Wy)[index] =charge;
	(*Vy)(index,index) = charge_err*charge_err/1e6;
	// fill covariance matrix as well
	// Vy ...
	// need to differentiate which plane it is
	// WirePlaneType_t plane = allwire[j]->plane();
	// Double_t charge_noise;
	// if (plane == 0){
	//   charge_noise = 14000*0.05;
	// }else if (plane==1){
	//   charge_noise = 14000*0.03;
	// }else if (plane==2){
	//   charge_noise = 14000*0.02;
	// }
	// (*Vy)(index,index) = (charge_noise*charge_noise + 0.05*0.05 * charge*charge)/1e6;
      }
    }
    
    
    GeomWireSelection allmwire = mergetiling.get_allwire();
    for (int j=0;j!=allmwire.size();j++){
      if (mwimap.find(allmwire[j])!=mwimap.end()){
	int index = mwimap[allmwire[j]];
	//construct MA
	for (int k=0; k!=mergetiling.cells(*allmwire[j]).size();k++){
	  int index1 = mcimap[mergetiling.cells(*allmwire[j]).at(k)];
	  (*MA)(index,index1) = 1;
	}
	
	//construct MB
	for (int k=0;k!=((MergeGeomWire*)allmwire[j])->get_allwire().size();k++){
	  int index1 = swimap[((MergeGeomWire*)allmwire[j])->get_allwire().at(k)];
	  (*MB)(index,index1) = 1;
	}
      }
    }
    
    
    // // construct the rest of matrix
    MBT->Transpose(*MB);
    MAT->Transpose(*MA);
    
    *VBy = (*MB) * (*Vy) * (*MBT);
    
    
    
    *VBy_inv = *VBy;
    //    if (fabs(VBy->Determinant())<0.01) {
    // std::cout << "Problem " << VBy->Determinant() << std::endl;
    //}
    VBy_inv->Invert();
    
    *MC = (*MAT) * (*VBy_inv) * (*MA);
    //double det = MC->Determinant();
    double det;
    if (mcindex <= mwindex){
      det = MC->Determinant();
    }else{
      det = 0;
    }
    solve_flag = 0;
    if (svd_flag==0){
      if (det > 1e-5)
	Solve();
    }else{
      if (det > 1e-5){
	Solve();
      }else{
	Solve_SVD();
      }
      //Solve_SVD();
    }
  }else{
    //    solve_flag = 1;
  }
  //MA->Print();
  // MAT->Print();
  // MB->Print();
  // MBT->Print();
  // Vy->Print();
  
  
}

/**
 * Builds up the indices for the ToyMatrix class.
 * 
 * This function initializes the indices for merged cells, merged wires, and single channels.
 * It iterates over all merged cells, assigns a unique index to each one, and then does the same for merged wires and single channels.
 * The function uses several maps to keep track of the assigned indices.
 * 
 * @param gds The DetectorGDS object.
 * @param mergetiling The MergeToyTiling object.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
void WCP2dToy::ToyMatrix::Buildup_index(const WCP::DetectorGDS& gds, WCP2dToy::MergeToyTiling& mergetiling){
  mcindex = 0; // merged cell
  mwindex = 0; // merged wire
  swindex = 0; // single channel
  
  GeomCellSelection allmcell = mergetiling.get_allcell();

  for (int j=0;j!=allmcell.size();j++){
    //construct merged cell index
    const MergeGeomCell *mcell = (MergeGeomCell*)allmcell[j];
    if (mcimap.find(mcell) == mcimap.end()){
      mcimap[mcell] = mcindex;
      mcindex ++;
      
      const GeomWireSelection wires = mergetiling.wires(*allmcell[j]);
       for (int k=0;k!=wires.size();k++){
	 const MergeGeomWire *mwire = (MergeGeomWire*)wires[k];
	 // require all the wire must be good to be used in the matrix solving 
	 if (mwimap.find(mwire) == mwimap.end() && mergetiling.wcmap()[mwire] >10){
	   //if (mwimap.find(mwire) == mwimap.end() ){
	   mwimap[mwire] = mwindex;
	   mwindex ++;
	   
	   //construct single channel index
	   GeomWireSelection swires = mwire->get_allwire();
	   for (int kk = 0; kk!=swires.size(); kk++){
	     const GeomWire* wire1 = swires[kk];
	     if (scimap.find(wire1->channel()) == scimap.end()){
	       scimap[wire1->channel()] = swindex;
	       swindex ++; 
	     }
	   }
	   
	 }
       }
    }
  }
}

/**
 * @brief Constructor for ToyMatrix class.
 * 
 * Initializes the ToyMatrix object with the given parameters.
 * 
 * @param gds Detector geometry and detector sensitive volume.
 * @param toytiling Toy tiling object.
 * @param mergetiling Merged toy tiling object.
 * @param recon_t Reconstruction threshold.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
WCP2dToy::ToyMatrix::ToyMatrix(const DetectorGDS& gds,WCP2dToy::ToyTiling& toytiling, WCP2dToy::MergeToyTiling& mergetiling, int recon_t){
  solve_flag = -1;
  chi2 = -1;
  svd_flag = 0;
  num_blob = 0;
  simple_blob_reduction = false;
  recon_threshold = recon_t;
  
  //building up index 
  Buildup_index(gds,mergetiling);
  //std::cout << mcindex << " " << mwindex << " " << swindex << std::endl;
  
  if (mcindex >0){
    //Construct Matrix
    MA = new TMatrixD(mwindex,mcindex);
    MB = new TMatrixD(mwindex,swindex);
    MAT = new TMatrixD(mcindex,mwindex);
    MBT = new TMatrixD(swindex,mwindex);
    
    Vy = new TMatrixD(swindex,swindex);
    VBy = new TMatrixD(mwindex,mwindex);
    VBy_inv = new TMatrixD(mwindex,mwindex);
    Vx = new TMatrixD(mcindex,mcindex);
    Vx_inv = new TMatrixD(mcindex,mcindex);
    
    MC = new TMatrixD(mcindex,mcindex);
    MC_inv = new TMatrixD(mcindex,mcindex);
    
    //Construct Vector
    Wy = new TVectorD(swindex);
    
    MWy = new TVectorD(mwindex);
    MWy_pred = new TVectorD(mwindex);
    
    Cx = new TVectorD(mcindex);
    dCx = new TVectorD(mcindex);
    
    std::map<int, float> ccmap = toytiling.ccmap();
    std::map<int, float> ccemap = toytiling.ccemap();
    
    for ( auto it = ccmap.begin(); it != ccmap.end(); it++){
      if (scimap.find(it->first) != scimap.end()){
	int index = scimap[it->first];
	float charge = ccmap[it->first];
	float charge_err = ccemap[it->first];
	(*Wy)[index] =charge;
	(*Vy)(index,index) = charge_err*charge_err/1e6;
      }
    }
    
    GeomWireSelection allmwire = mergetiling.get_allwire();
    for (int j=0;j!=allmwire.size();j++){
      if (mwimap.find(allmwire[j])!=mwimap.end()){
	int index = mwimap[allmwire[j]];
	//construct MA
	for (int k=0; k!=mergetiling.cells(*allmwire[j]).size();k++){
	  int index1 = mcimap[mergetiling.cells(*allmwire[j]).at(k)];
	  (*MA)(index,index1) = 1;
	}

	//construct MB
	for (int k=0;k!=((MergeGeomWire*)allmwire[j])->get_allwire().size();k++){
	  int index1 = scimap[((MergeGeomWire*)allmwire[j])->get_allwire().at(k)->channel()];
	  (*MB)(index,index1) = 1;
	}
      }
    }

    // construct the rest of matrix
    MBT->Transpose(*MB);
    MAT->Transpose(*MA);
    
    *VBy = (*MB) * (*Vy) * (*MBT);
    *VBy_inv = *VBy;
    VBy_inv->Invert();
    
    *MC = (*MAT) * (*VBy_inv) * (*MA);
    solve_flag = 0;
    
    double det;
    if (mcindex <= mwindex){
      det = MC->Determinant();
    }else{
      det = 0;
    }
    if (det > 1e-5){
      Solve();
    }

  }else{
    //    solve_flag = 1;
  }
}

/**
 * @brief Constructor for ToyMatrix class.
 * 
 * @param toytiling Reference to ToyTiling object.
 * @param mergetiling Reference to MergeToyTiling object.
 * @param recon_t Reconstruction threshold value.
 
WCP2dToy::ToyMatrix::ToyMatrix(WCP2dToy::ToyTiling& toytiling, WCP2dToy::MergeToyTiling& mergetiling, int recon_t){
   * @brief Initializes member variables.
 
  solve_flag = -1;
  chi2 = -1;
  svd_flag = 0;
  num_blob = 0;
  simple_blob_reduction = false;
   * @brief Builds up index for ToyMatrix construction.
 * 
 * @param mergetiling Reference to MergeToyTiling object.
 
  Buildup_index(mergetiling);
  recon_threshold = recon_t;
   * @brief Checks if mcindex is greater than 0.
 
  if (mcindex >0){ * @brief Constructs matrices and vectors for ToyMatrix.
 
    MA = new TMatrixD(mwindex,mcindex);
    MB = new TMatrixD(mwindex,swindex);
    MAT = new TMatrixD(mcindex,mwindex);
    MBT = new TMatrixD(swindex,mwindex);
    
    Vy = new TMatrixD(swindex,swindex);
    VBy = new TMatrixD(mwindex,mwindex);
    VBy_inv = new TMatrixD(mwindex,mwindex);
    Vx = new TMatrixD(mcindex,mcindex);
    Vx_inv = new TMatrixD(mcindex,mcindex);
    
    MC = new TMatrixD(mcindex,mcindex);
    MC_inv = new TMatrixD(mcindex,mcindex);
     * @brief Constructs vectors for ToyMatrix.
 
    Wy = new TVectorD(swindex);
    
    MWy = new TVectorD(mwindex);
    MWy_pred = new TVectorD(mwindex);

    Cx = new TVectorD(mcindex);
    dCx = new TVectorD(mcindex);
     * @brief Gets wire selections and charge maps from ToyTiling object.
 
    GeomWireSelection allwire = toytiling.get_allwire();
    WireChargeMap wcmap = toytiling.wcmap();
    WireChargeMap wcemap = toytiling.wcemap();
     * @brief Loops through all wires and fills matrices and vectors.
 
    for (int j=0;j!=allwire.size();j++){
      if (swimap.find(allwire[j])!=swimap.end()){
        int index = swimap[allwire[j]];
        float charge = wcmap[allwire[j]];
        float charge_err = wcemap[allwire[j]];
        (*Wy)[index] =charge;
        (*Vy)(index,index) = charge_err*charge_err/1e6;
      }
    }
     * @brief Gets merged wire selection from MergeToyTiling object.
 
    GeomWireSelection allmwire = mergetiling.get_allwire();
     * @brief Loops through all merged wires and constructs matrices.
 
    for (int j=0;j!=allmwire.size();j++){
      if (mwimap.find(allmwire[j])!=mwimap.end()){
        int index = mwimap[allmwire[j]];
        for (int k=0; k!=mergetiling.cells(*allmwire[j]).size();k++){
          int index1 = mcimap[mergetiling.cells(*allmwire[j]).at(k)];
          (*MA)(index,index1) = 1;
        }
        
        for (int k=0;k!=((MergeGeomWire*)allmwire[j])->get_allwire().size();k++){
          int index1 = swimap[((MergeGeomWire*)allmwire[j])->get_allwire().at(k)];
          (*MB)(index,index1) = 1;
        }
      }
    }
     * @brief Transposes and multiplies matrices.
 
    MBT->Transpose(*MB);
    MAT->Transpose(*MA);
    
    *VBy = (*MB) * (*Vy) * (*MBT);
    
    *VBy_inv = *VBy;
    VBy_inv->Invert();
    
    *MC = (*MAT) * (*VBy_inv) * (*MA);
    solve_flag = 0;
    
    double det;
    if (mcindex <= mwindex){
      det = MC->Determinant();
    }else{
      det = 0;
    }
    if (det > 1e-5){
      Solve();
    }
  }else{
    solve_flag = 1;
  }
}* This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
WCP2dToy::ToyMatrix::ToyMatrix(WCP2dToy::ToyTiling& toytiling, WCP2dToy::MergeToyTiling& mergetiling, int recon_t){
  solve_flag = -1;
  chi2 = -1;
  svd_flag = 0;
  num_blob = 0;
  simple_blob_reduction = false;
  // build up index
  Buildup_index(mergetiling);
  recon_threshold = recon_t;
  
  //std::cout << mcindex << " " << mwindex << " " << swindex << std::endl;
  if (mcindex >0){
  //Construct Matrix
    MA = new TMatrixD(mwindex,mcindex);
    MB = new TMatrixD(mwindex,swindex);
    MAT = new TMatrixD(mcindex,mwindex);
    MBT = new TMatrixD(swindex,mwindex);
    
    Vy = new TMatrixD(swindex,swindex);
    VBy = new TMatrixD(mwindex,mwindex);
    VBy_inv = new TMatrixD(mwindex,mwindex);
    Vx = new TMatrixD(mcindex,mcindex);
    Vx_inv = new TMatrixD(mcindex,mcindex);
    
    MC = new TMatrixD(mcindex,mcindex);
    MC_inv = new TMatrixD(mcindex,mcindex);
    
    //Construct Vector
    Wy = new TVectorD(swindex);
    
    MWy = new TVectorD(mwindex);
    MWy_pred = new TVectorD(mwindex);

    Cx = new TVectorD(mcindex);
    dCx = new TVectorD(mcindex);
    
    GeomWireSelection allwire = toytiling.get_allwire();
    WireChargeMap wcmap = toytiling.wcmap();
    WireChargeMap wcemap = toytiling.wcemap();
    for (int j=0;j!=allwire.size();j++){
      if (swimap.find(allwire[j])!=swimap.end()){
	int index = swimap[allwire[j]];
	float charge = wcmap[allwire[j]];
	float charge_err = wcemap[allwire[j]];
	(*Wy)[index] =charge;
	(*Vy)(index,index) = charge_err*charge_err/1e6;
	// fill covariance matrix as well
	// Vy ...
	// need to differentiate which plane it is
	// WirePlaneType_t plane = allwire[j]->plane();
	// Double_t charge_noise;
	// if (plane == 0){
	//   charge_noise = 14000*0.05;
	// }else if (plane==1){
	//   charge_noise = 14000*0.03;
	// }else if (plane==2){
	//   charge_noise = 14000*0.02;
	// }
	// (*Vy)(index,index) = (charge_noise*charge_noise + 0.05*0.05 * charge*charge)/1e6;
      }
    }
    
    
    GeomWireSelection allmwire = mergetiling.get_allwire();
    for (int j=0;j!=allmwire.size();j++){
      if (mwimap.find(allmwire[j])!=mwimap.end()){
	int index = mwimap[allmwire[j]];
	//construct MA
	for (int k=0; k!=mergetiling.cells(*allmwire[j]).size();k++){
	  int index1 = mcimap[mergetiling.cells(*allmwire[j]).at(k)];
	  (*MA)(index,index1) = 1;
	}
	
	//construct MB
	for (int k=0;k!=((MergeGeomWire*)allmwire[j])->get_allwire().size();k++){
	  int index1 = swimap[((MergeGeomWire*)allmwire[j])->get_allwire().at(k)];
	  (*MB)(index,index1) = 1;
	}
      }
    }
    
    
    // // construct the rest of matrix
    MBT->Transpose(*MB);
    MAT->Transpose(*MA);
    
    *VBy = (*MB) * (*Vy) * (*MBT);
    
    //    std::cout << "fill in the data" << std::endl;

    *VBy_inv = *VBy;
    //    if (fabs(VBy->Determinant())<0.01) {
    // std::cout << "Problem " << VBy->Determinant() << std::endl;
    //}
    VBy_inv->Invert();
    
    //std::cout << "invert the matrix " << std::endl; 

    
    *MC = (*MAT) * (*VBy_inv) * (*MA);
    solve_flag = 0;
    
    double det;
    if (mcindex <= mwindex){
      det = MC->Determinant();
    }else{
      det = 0;
    }
    if (det > 1e-5){
      Solve();
    }
  }else{
    //    solve_flag = 1;
  }
  //MA->Print();
  // MAT->Print();
  // MB->Print();
  // MBT->Print();
  // Vy->Print();

  
}



/**
 * @brief Updates prediction values in the toy matrix object
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
void WCP2dToy::ToyMatrix::Update_pred(){
  *MWy = (*MB) * (*Wy);
  *MWy_pred = (*MA) * (*Cx);
}

/**
 * @brief Prints the contents of the ToyMatrix object.
 *
 * This function prints the matrix A, followed by a newline character,
 * and then prints the vector Cx. It also iterates over the wire indices,
 * printing the index, measured value, predicted value, and uncertainty
 * for each wire.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
void WCP2dToy::ToyMatrix::Print(){

  MA->Print();
  std::cout << std::endl;
  Cx->Print();

  for (int i=0;i!=mwindex;i++){
    std::cout << "wire: " << i << " " << (*MWy)[i] << " " << (*MWy_pred)[i] << " " << sqrt((*VBy)[i][i]) << std::endl;
  }
}

/**
 * @brief Calculates the residual value for a given cell in the toy matrix.
 *
 * This function computes the residual by summing the product of the absolute difference between measured and predicted values with the corresponding matrix element.
 *
 * @param cell The geometric cell for which the residual is calculated.
 * @return The calculated residual value.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
double WCP2dToy::ToyMatrix::Get_residual(const WCP::GeomCell *cell){
  double res=0;
  int index = mcimap[cell];

  for (int i=0;i!=mwindex;i++){
    res += (*MA)(i,index) * fabs((*MWy)[i] - (*MWy_pred)[i]);
  }
  
  return res;
}

/**
 * @brief Destructor for ToyMatrix class
 * 
 * Releases dynamically allocated memory for various matrices and vectors
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
WCP2dToy::ToyMatrix::~ToyMatrix(){
  
  
  delete MA;
  delete MB;
  delete MAT;
  delete MBT;
  delete Vy;
  delete VBy;
  delete Vx;
  delete VBy_inv;
  delete Vx_inv;
  delete MC;
  delete MC_inv;
  delete Wy;
  delete Cx;
  delete dCx;
  delete MWy_pred;
  delete MWy;
}

/**
 * @brief Solves the matrix equation.
 * 
 * This function solves the matrix equation and calculates the solution vector,
 * its error, chi-squared value, and the number of degrees of freedom.
 * 
 * @return The solve flag indicating whether the solution was successful.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
int WCP2dToy::ToyMatrix::Solve(){
  
  // double det = MC->Determinant();

  //  std::cout << det << std::endl;
  
  //if (fabs(det)>1e-5){
    *MC_inv = *MC;
    MC_inv->Invert();
    *Cx = (*MC_inv) * (*MAT) * (*VBy_inv) * (*MB) * (*Wy);
    
    *Vx_inv = (*MAT) * (*VBy_inv) * (*MA);
    *Vx = *Vx_inv;
    Vx->Invert();
    
    for (int i=0;i!=mcindex;i++){
      (*dCx)[i] = sqrt( (*Vx)(i,i)) * 1000.;
    }
    
    TVectorD sol = (*MB) * (*Wy) - (*MA) * (*Cx);
    TVectorD sol1 =  (*VBy_inv) * sol;
    chi2 = sol * (sol1)/1e6;
    
    ndf = mwindex - mcindex;
    
    //std::cout << chi2 << std::endl;
    //      for (int i=0;i!=mcindex;i++){
    //	std::cout << (*Cx)[i] << " " << (*dCx)[i]*1000. << std::endl;
    //}
    
    solve_flag = 1;
    //}
  
  return solve_flag;
}

/**
 * @brief Solves the system of linear equations using Singular Value Decomposition.
 *
 * This function uses SVD to solve the matrix equation and calculates the solution vector,
 * error vector, and chi-squared value.
 *
 * @return The solve flag indicating whether the solution was successful.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
int WCP2dToy::ToyMatrix::Solve_SVD(){
  
  // if (svd_removed.size()!=0){
  //   //update *MC
  //   int n_removed = svd_removed.size();
  //   TMatrixD MA2(mwindex,mcindex - n_removed);
  //   TMatrixD MA2T(mcindex - n_removed,mwindex);
    
  //   int index2 = 0;
  //   for (int k=0;k!=mcindex;k++){ //loop all possibility
  //     auto it3 = find(svd_removed.begin(),svd_removed.end(),k);
  //     if (it3 != svd_removed.end()){ // not in removed
  // 	for (int j=0;j!=mwindex;j++){
  // 	  MA2(j,index2) = (*MA)(j,k);
  // 	}
  // 	index2 ++;
  //     }
  //   }
  //   MA2T.Transpose(MA2);
  //   TMatrixD MC2(mcindex-svd_removed.size(),mcindex-svd_removed.size());
  //   TMatrixD MC2_inv(mcindex-svd_removed.size(),mcindex-svd_removed.size());

  //   MC2 = MA2T * (*VBy_inv) * MA2;
  //   TDecompSVD svd(MC2);
  //   MC2_inv = svd.Invert();
     
        
  //   TVectorD Cxt(mcindex-n_removed);
  //   TVectorD dCxt(mcindex-n_removed);
  //   TMatrixD Vxt(mcindex-n_removed,mcindex-n_removed);
  //   TMatrixD Vxt_inv(mcindex-n_removed,mcindex-n_removed);
    
  //   Cxt = (MC2_inv) * (MA2T) * (*VBy_inv) * (*MB) * (*Wy);
  //   Vxt_inv = (MA2T) * (*VBy_inv) * (MA2);
  //   Vxt = Vxt_inv;
  //   Vxt.Invert();
    
  //   for (int i=0;i!=mcindex-n_removed;i++){
  //     dCxt[i] = sqrt( Vxt(i,i)) * 1000.;
  //   }

  //   TVectorD sol = (*MB) * (*Wy) - (MA2) * (Cxt);
  //   TVectorD sol1 =  (*VBy_inv) * sol;
  //   chi2 = sol * (sol1)/1e6;
  //   ndf = mwindex - mcindex + n_removed;
  //   for (int i=0;i!=mcindex-n_removed;i++){
  //     if (Cxt[i] <0){
  // 	chi2 += 10*pow(Cxt[i]/dCxt[i],2);
  //     }
  //   }

  //   int index = 0;
  //   for (int i=0;i!=mcindex;i++){
  //     auto it = find(svd_removed.begin(),svd_removed.end(),i);
  //     if (it==svd_removed.end()){
  // 	(*Cx)[i] = Cxt[index];
  // 	(*dCx)[i] = dCxt[index];
  // 	index ++;
  //     }else{
  // 	(*Cx)[i] = 0.;
  // 	(*dCx)[i] = 0.;
  //     }
  //   }


  // }else{
  TDecompSVD svd(*MC);
  *MC_inv = svd.Invert();
  *Cx = (*MC_inv) * (*MAT) * (*VBy_inv) * (*MB) * (*Wy);
  *Vx_inv = (*MAT) * (*VBy_inv) * (*MA);
  *Vx = *Vx_inv;
  Vx->Invert();
  for (int i=0;i!=mcindex;i++){
    (*dCx)[i] = sqrt( (*Vx)(i,i)) * 1000.;
    if ((*Cx)[i]<0) svd_removed.push_back(i);
  }
  TVectorD sol = (*MB) * (*Wy) - (*MA) * (*Cx);
  TVectorD sol1 =  (*VBy_inv) * sol;
  //chi2 = sol * (sol1)/1e6;
  //ndf = mwindex - mcindex;
  solve_flag = 0;
  //}
    
  return solve_flag;
}



/**
 * @brief Returns cell charge or uncertainty value
 * @param cell Pointer to GeomCell object
 * @param flag Flag indicating whether to retrieve charge or uncertainty
 * @return Charge or uncertainty value of the cell
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
double WCP2dToy::ToyMatrix::Get_Cell_Charge( const WCP::GeomCell *cell, int flag )  {
  // flag == 1 charge
  // flag == 2 uncertainty
  int index = mcimap[cell];
  if (flag==1){
    return (*Cx)[index];
  }else if (flag==2){
    return (*dCx)[index];
  }
}


/**
 * Builds up indices for ToyMatrix class.
 *
 * This function initializes and constructs indices for merged cells, 
 * merged wires, and single wires within the MergeToyTiling object.
 *
 * It iterates over all merged cells, assigning unique indices to each,
 * and further assigns indices to their constituent merged wires and 
 * single wires, while ensuring that only valid wires with sufficient charge 
 * are included in the indexing process.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.5.
*/ 
void WCP2dToy::ToyMatrix::Buildup_index(WCP2dToy::MergeToyTiling& mergetiling){

  mcindex = 0;
  mwindex = 0;
  swindex = 0;

  GeomCellSelection allmcell = mergetiling.get_allcell();
 
  for (int j=0;j!=allmcell.size();j++){
    //construct merged cell index
    const MergeGeomCell *mcell = (MergeGeomCell*)allmcell[j];
    if (mcimap.find(mcell) == mcimap.end()){
      mcimap[mcell] = mcindex;
      mcindex ++;
      
      const GeomWireSelection wires = mergetiling.wires(*allmcell[j]);
      // cout << wires.size() << endl;
      for (int k=0;k!=wires.size();k++){
	//construct merged wire index
	const MergeGeomWire *mwire = (MergeGeomWire*)wires[k];
	// require all the wire must be good to be used in the matrix solving 
	if (mwimap.find(mwire) == mwimap.end() && mergetiling.wcmap()[mwire] >10){
	  //if (mwimap.find(mwire) == mwimap.end() ){
	  mwimap[mwire] = mwindex;
	  mwindex ++;
	  
	  //construct single wire index
	  GeomWireSelection swires = mwire->get_allwire();
	  for (int kk = 0; kk!=swires.size(); kk++){
	    const GeomWire* wire1 = swires[kk];
	    if (swimap.find(wire1) == swimap.end()){
	      swimap[wire1] = swindex;
	      swindex ++;
	    }
	  }
	  
	}
      }
      
    }
  }
  
  //std::cout << mcindex << " " << mwindex << " " << swindex << std::endl;
}


